<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportsSync">

        <!-- Student Dashboard with Bottom Nav -->
        <activity
            android:name=".StudentDashboardActivity"
            android:exported="true" />

        <!-- PT Dashboard -->
        <activity android:name=".PtDashboardActivity" />

        <!-- Login is launcher activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Student Profile Screen -->
        <activity android:name=".StudentProfileFragment" />

        <!-- Reports / Main -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Single FileProvider definition -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity
            android:name=".QrScanActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".CustomCaptureActivity"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:exported="false" />

        <activity
            android:name=".AdminApprovalRequestsActivity"
            android:exported="true" />

        <!-- Auto Logout Service -->
        <service android:name=".AutoLogoutService" />
        
        <!-- Logout Receiver -->
        <receiver android:name=".LogoutReceiver" />

    </application>

</manifest>
