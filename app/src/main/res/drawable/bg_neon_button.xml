<?xml version="1.0" encoding="utf-8"?>
<!-- Enhanced Neon Button with Glow Effect - Nothing UI -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer Glow Layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/neon_cyan_glow" />
            <corners android:radius="14dp" />
        </shape>
    </item>
    
    <!-- Middle Glow Layer -->
    <item 
        android:left="1dp"
        android:right="1dp"
        android:top="1dp"
        android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/neon_pulse" />
            <corners android:radius="13dp" />
        </shape>
    </item>
    
    <!-- Main Button Surface -->
    <item 
        android:left="2dp"
        android:right="2dp"
        android:top="2dp"
        android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/accent" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</layer-list>
